apiVersion: v1
kind: Service
metadata:
  labels:
    app: notification-system-broker
    environment: Development
  name: notification-system-broker
  namespace: services
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    app: notification-system-broker
  sessionAffinity: None
  type: ClusterIP
